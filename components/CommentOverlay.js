//TODO make it so if press comment on each post
//pop out this overlay from bottom
import React from 'react';

import {
  Box
} from '@chakra-ui/react';

//TODO finish this
export const CommentOverLay = ({ isVisible }) => {
  return(
    <Box></Box>
  );
};